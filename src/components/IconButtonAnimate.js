import { Box } from '@mui/material';

export function IconButtonAnimate() {
  return <div>IconButtonAnimate</div>;
}

export function AnimateWrap({ size, children }) {
  return <Box>{children}</Box>;
}
